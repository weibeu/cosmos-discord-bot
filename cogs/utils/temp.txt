settings = {}
for g in bot.guilds:
    guild = bot.db_client.guilds[str(g.id)]
    s = await guild.find_one({'_id': 'settings'})
    if s is None:
        pass
    else:
        try:
            settings[str(g.id)] = s["welcome"]
        except:
            continue
return settings