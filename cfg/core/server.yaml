# ! Configurations for internal aiohttp server.

host: 127.0.0.1
port: 7000

# Few secret keys required to request to endpoints.

DBLHOOK_KEY: ""               # ENV: DBLHOOK_KEY
PATREON_WEBHOOK_SECRET: ""    # ENV: PATREON_WEBHOOK_SECRET
