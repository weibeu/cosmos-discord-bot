# ! Configs for image processor client used to connect to image processing server.

base_url: "http://ec2-3-91-180-151.compute-1.amazonaws.com"    # If left empty, image_processor_client will try to use default server.
