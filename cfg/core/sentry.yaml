# ! Sentry error monitoring configurations.

dsn: ""    # ENV: SENTRY_DSN
debug: False
# release: "0.0.3 Babu"    # Dynamically set it using __release__ attribute of __init__.
environment: "Production"
